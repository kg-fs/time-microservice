{
  "name": "time-microservice",
  "version": "1.0.0",
  "description": "Microservice for calculating country local time and difference relative to Nicaragua",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
